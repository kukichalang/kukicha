# internal/ — Compiler Internals

For the full compiler reference (lexer, parser, AST, semantic, IR, codegen), invoke the **`/compiler-internals`** skill.

Pipeline: `lexer/` → `parser/` → `semantic/` → `codegen/` (via IR lowerer)
